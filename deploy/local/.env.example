# AWS Credentials
# Get these from Terraform output after deploying infrastructure:
#   cd ../terraform
#   terraform output -raw access_key_id
#   terraform output -raw secret_access_key

AWS_ACCESS_KEY_ID=your-access-key-here
AWS_SECRET_ACCESS_KEY=your-secret-access-key-here
AWS_DEFAULT_REGION=ap-northeast-1

# KVS Configuration
STREAM_NAME=dockerStream
TCP_PORT=5000

# TCP Host Configuration (optional)
# Leave empty for auto-detection
# Linux: will use 172.17.0.1
# macOS/Windows: will use host.docker.internal
TCP_HOST=172.17.0.1
